go to the directory and  run "make run" in console to run.
"./bin/exe" to run already compiled code.
to clear executable files run "make dump"

"W" to increase speed
"S" to decrease speed

Click on the object and "M" to change mapping
Click on the object and "N" to change Texture type